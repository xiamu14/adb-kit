[project]
name = "adb-wifi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["qrcode>=8.2", "zeroconf>=0.148.0"]

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[project.scripts]
ak = "adb_wifi:main"
